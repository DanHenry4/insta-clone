const amqp = require('amqplib');

// This file is a stub to allow importing the shared rabbitmq utility from the project root.
// In a real deployment, you might use a package manager or symlink for shared code.
module.exports = require('../../utils/rabbitmq');
